<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xyz.nullicn.skytakeserver.mapper.ShoppingCartMapper">


    <select id="getByUserIdAndDishIdAndFlavor" resultType="xyz.nullicn.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            and user_id = #{userId}
            <if test="dishId != null">and dish_id = #{dishId}</if>
            <if test="setmealId != null">and setmeal_id = #{setmealId}</if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
            <if test="dishFlavor == null">
                and dish_flavor is null
            </if>
        </where>
    </select>

    <update id="updateNumberById">
        update shopping_cart set number = #{number} where id = #{id}
    </update>

    <insert id="insert" parameterType="xyz.nullicn.entity.ShoppingCart"
            useGeneratedKeys="true" keyProperty="id">
        insert into shopping_cart (name, user_id, dish_id, setmeal_id, dish_flavor,
                                   number, amount, image, create_time)
        values (#{name}, #{userId}, #{dishId}, #{setmealId}, #{dishFlavor},
                #{number}, #{amount}, #{image}, #{createTime})
    </insert>
</mapper>