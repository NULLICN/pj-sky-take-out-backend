<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xyz.nullicn.skytakeserver.mapper.OrderDetailMapper">

    <insert id="insert">
        INSERT INTO order_detail (name, order_id, dish_id, setmeal_id, dish_flavor, number, amount, image)
        VALUES
        <foreach collection="orderDetail" item="item" separator=",">
            (#{item.name}, #{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor}, #{item.number}, #{item.amount}, #{item.image})
        </foreach>
    </insert>

    <select id="getTop10Sales" resultType="java.util.HashMap">
        SELECT od.name, SUM(od.number) AS total_number
        FROM order_detail od
                 JOIN orders o ON od.order_id = o.id
        WHERE o.status = #{status}
          AND o.order_time &gt;= #{begin}
          AND o.order_time &lt; #{end}
          AND EXISTS (SELECT 1 FROM employee e WHERE e.id = #{employeeId})
        GROUP BY od.name
        ORDER BY total_number DESC
        LIMIT 10
    </select>
</mapper>
